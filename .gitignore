# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code local-only state
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/skills/

# Node / TS (will apply once apps/web and packages/sim land)
node_modules/
dist/
.turbo/
.next/
*.log

# Rust (will apply once services/prover and contracts/flight_scroll land)
target/
**/*.rs.bk
Cargo.lock.bak

# Env / secrets
.env
.env.local
.env.*.local

# Relay runtime state
services/server/.data/

# Build artifacts
proof_artifacts.json
*.wasm.gz
.deploy-state*
.deploy-keys.*

# playwright smoke test artifacts
apps/web/tests/screenshots/

# MCP server configs (may contain API tokens)
.mcp.json
.vercel
