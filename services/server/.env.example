# Copy to .env, fill in for your deploy. .env is gitignored.

# HTTP listen port.
PORT=8787

# Stellar network the relay submits txs on. testnet | mainnet
NETWORK=testnet

# Soroban RPC endpoint + matching passphrase.
STELLAR_RPC_URL=https://soroban-testnet.stellar.org
STELLAR_NETWORK_PASSPHRASE="Test SDF Network ; September 2015"

# Deployed game_hub contract id (C...). From .deploy-state.<network>.
GAME_HUB_CONTRACT_ID=

# Relay's own Stellar secret key (S...). Pays gas for submit_score txs.
# This account does NOT need any admin role on the contract — submit_score
# is permissionless. It just needs XLM for fees.
RELAY_SECRET_KEY=

# game_id the relay submits scores against. flight_scroll = 1.
GAME_ID=1

# Path to the compiled flight-host binary (built via `cargo build --release
# --bin flight-host` from repo root). The relay spawns this per request.
FLIGHT_HOST_BIN=../../target/release/flight-host

# Per-pubkey cooldown between submissions, in seconds. Prevents one player
# from spamming the relay and draining its XLM balance. 0 disables.
SUBMIT_COOLDOWN_SECONDS=60
